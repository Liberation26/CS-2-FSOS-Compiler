# Oryn 0.4.4

## Fixes

- Updated `update.sh` to force the GitHub origin remote to `https://github.com/Liberation26/CS-2-FSOS-Compiler.git`.
- Removed the stale `https://github.com/Liberation26/OrynFoundry` remote from all shipped updater copies.
- Kept the Stage 4 approved module boundary work from 0.4.x unchanged.

## Packaging

- Bumped archive version to `0.4.4`.
- Included the fixed updater at the archive root, in `ChangedFiles/update.sh`, and in `FullSource/update.sh`.
