# Oryn 0.4.1

## Fixes

- Fixed the Stage 4 QEMU proof check so it accepts the approved module boundary entry diagnostic that the Stage 4 kernel emits.
- Added the standard `Stage4 kernel entered` and `Stage4 kernel is halting forever` proof lines to the Stage 4 kernel for consistency with the Stage 2 and Stage 3 harness checks.
- Kept compatibility with the first Stage 4 proof line so existing Stage 4 serial logs still validate the approved module boundary entry path.
