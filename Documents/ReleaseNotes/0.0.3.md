# Oryn 0.0.4

## Fixes

- `update.sh` now displays its own version number immediately at startup.
- Existing `~/Dev/OrynFoundry` directories that are not Git repositories are initialised in place instead of failing.
- The updater still extracts the selected archive to `/tmp`, copies `ChangedFiles/` into `~/Dev/OrynFoundry`, commits locally, and attempts to push to `https://github.com/Liberation26/CS-2-FSOS-Compiler.git`.
