# Oryn 0.0.2 Release Notes

## Fixes

- Fixed `update.sh` so an existing `~/Dev/OrynFoundry` directory without a `.git/` folder is initialised as a Git repository in place instead of failing.
- The updater still extracts the downloaded archive to `/tmp`, locates `ChangedFiles/`, copies it into `~/Dev/OrynFoundry`, stages the result, commits it, and attempts to push to `https://github.com/Liberation26/CS-2-FSOS-Compiler.git`.
- Updated the archive version to `0.0.2`.
