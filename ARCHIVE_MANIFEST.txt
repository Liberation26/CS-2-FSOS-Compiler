Oryn archive version: 0.2.4

This archive contains:

- FullSource/  complete source tree snapshot for Oryn 0.2.4
- ChangedFiles/ files changed since Oryn 0.2.3

Primary change:

- Runqemu.sh compiler build step now shows live dotnet build output, writes Build/Oryn.Compiler.build.log, uses --disable-build-servers, and fails clearly on timeout/failure instead of appearing to stop silently.
