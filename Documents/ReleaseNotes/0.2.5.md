# Oryn 0.2.5

Fixes the Stage 2 phase 2 compiler refactor build by removing an invalid namespace import from `CompilerPipeline.cs`.

## Fixed

- `CompilerPipeline.cs` no longer imports `Oryn.Compiler.Frontend.SemanticModel` because the semantic binding records are currently declared in the compiler root namespace while physically stored under `Frontend/SemanticModel/`.
- Keeps the Stage 2 separation layout intact while allowing the compiler project to build.

## Notes

This is a small repair release for the 0.2.3/0.2.4 frontend separation work.
